---
import { getCollection } from "astro:content";
export const getStaticPaths = async () => {
  return (await getCollection("characters")).map((item) => ({
    params: { slug: item.data.slug },
  }));
};
---
